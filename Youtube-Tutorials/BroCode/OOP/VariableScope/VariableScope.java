public class VariableScope {
    public static void main(String[] args) {
        // local varaible = Declared inside a method and is visible only to that method.
        // global variable = Declare outside a method, but within a class visible to all parts of a class.

        
    }
}
